name: Publish Dev Types Package

on:
  # Trigger after successful Development deploy
  workflow_run:
    workflows: ['Deploy to Development']
    types:
      - completed
    branches: [develop]

  # Allow manual triggering for immediate publishing
  workflow_dispatch: {}

jobs:
  publish-dev:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch'

    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link to Development project
        run: supabase link --project-ref ${{ secrets.SUPABASE_DEV_PROJECT_REF }} --password ${{ secrets.SUPABASE_DEV_DB_PASSWORD }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Generate types from Development
        run: |
          echo "🔄 Generating types from development database..."
          supabase gen types typescript --linked > types/database.ts
          echo "✅ Types generated successfully"

      - name: Build package artifacts
        run: npm run prepare-package:ci

      - name: Set prerelease dev version (no repo mutations)
        id: set_version
        run: |
          BASE=$(node -p "require('./package.json').version.split('-')[0]")
          TS=$(date +%Y%m%d.%H%M%S)
          DEV_VERSION="${BASE}-dev.${TS}.${GITHUB_RUN_NUMBER}"
          echo "Setting version to $DEV_VERSION"
          npm version "$DEV_VERSION" --no-git-tag-version
          echo "version=$DEV_VERSION" >> $GITHUB_OUTPUT

      - name: Publish to NPM as dev tag
        run: |
          VERSION="${{ steps.set_version.outputs.version }}"
          echo "🚀 Publishing @everylanguage/shared-types@$VERSION (tag: dev) to NPM..."
          # Skip if version already exists on NPM (in case of retries)
          if npm view @everylanguage/shared-types@$VERSION version >/dev/null 2>&1; then
            echo "⚠️ Version $VERSION already exists on NPM, skipping publish"
          else
            npm publish --access public --tag dev
            echo "✅ Package published successfully as 'dev'"
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summary
        run: |
          echo "📦 Dev types published with tag 'dev'."
          echo "Consumers can install using: npm i @everylanguage/shared-types@dev"


